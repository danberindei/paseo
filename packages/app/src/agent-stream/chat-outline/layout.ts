// Rail geometry shared by the rail itself (rail.web.tsx) and the transcript gutter that
// view.tsx reserves so content never sits under the rail's click column.
export const RAIL_WIDTH = 36;
export const MIN_PANEL_WIDTH = 918;
// rail left (spacing[2] = 8) + RAIL_WIDTH + one spacing[2] gap.
export const RAIL_GUTTER = 52;
